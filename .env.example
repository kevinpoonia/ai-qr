# Secret used to sign admin session cookies. Generate your own with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Changing this value logs every admin session out.
SESSION_SECRET=

# Postgres connection string. A Neon project's pooled connection string works
# well here (Settings > Connection Details in the Neon dashboard).
DATABASE_URL=
